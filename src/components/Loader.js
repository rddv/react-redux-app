import React from 'react';
import PropTypes from 'prop-types';

function Loader(props) {
    return (
        <h2>Loading...</h2>
    );
}

Loader.propTypes = {};
Loader.defaultProps = {};

export default Loader;
